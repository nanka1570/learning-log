# 学習ログ

技術学習で得た知識の蓄積。自分の言葉で説明→正誤確認のプロセスを記録。

## 「理解した」の定義

以下の3段階をクリアして初めて「理解した」とみなす。

1. **自分の言葉で説明できる**
2. **「なぜ？」「それで防げるの？」と突っ込まれても答えられる**
3. **弱点・限界まで説明できる**

---

## [001] サーバ証明書の検証プロセス
- **日付:** 2026-02-13(金)
- **カテゴリ:** セキュリティ / 暗号技術
- **プロジェクト:** 情報処理安全確保支援士
- **自分の理解:** サーバ証明書では、認証局で証明書のメッセージダイジェストをハッシュ関数で作り出し、それを秘密鍵で暗号化する。クライアントでは認証局の公開鍵で復号し、取り出したメッセージダイジェストとは別に、サーバ証明書からハッシュ関数でメッセージダイジェストを作り出し、この2つのメッセージダイジェストで改ざんされていないか確認する。
- **質問:** 認証局とクライアントでハッシュ方法をどう合わせているか？
- **答え:** 証明書の中に「署名アルゴリズム」フィールドが平文で書いてあり、クライアントはそれを読んで同じ方法で検証する
- **正誤:** 正解（追加理解あり）
- **補足:** 
  - 証明書の本体部分は暗号化されておらず、署名値のみが暗号化されている
  - セキュリティの原則（ケルクホフスの原理）：アルゴリズムが公開されていても、鍵さえ秘密なら安全
  - アルゴリズムを公開することで、脆弱性発見時の移行や相互運用性が確保できる
- **実務での使用頻度:** 高（HTTPS通信で毎回自動的に行われる）
- **関連:** なし

---

## [002] DH法（Diffie-Hellman鍵交換）
- **日付:** 2026-02-16(月)
- **カテゴリ:** セキュリティ / 暗号技術
- **プロジェクト:** 情報処理安全確保支援士
- **自分の理解:** DH法は、公開しても良い素数と整数を作って、公開しない乱数をそれぞれで作り素数と整数から共通鍵を作り出すハイブリッド暗号方式
- **正誤:** 一部修正
- **補足:**
  - DH法は「鍵交換プロトコル」であり、ハイブリッド暗号方式ではない
  - 用語の整理：
    - 鍵交換: 共通鍵を安全に共有する方法（DH法、ECDH）
    - ハイブリッド暗号: 公開鍵で共通鍵を送り、その共通鍵でデータを暗号化（SSL/TLS）
  - DH法の仕組み：
    - 公開パラメータ（素数p、生成元g）
    - 秘密の乱数（a, b）を各自が生成
    - 公開値（g^a mod p, g^b mod p）を交換
    - 共通鍵（g^(ab) mod p）を各自が計算
  - 盗聴者は公開情報だけでは共通鍵を計算できない（離散対数問題）
  - DH法とハイブリッド暗号の共通点：どちらも「共通鍵の配送問題」を解決する技術
- **実務での使用頻度:** 高（HTTPS通信のTLSでECDHが使われている）
- **関連:** [001]（証明書と組み合わせて使用）

---

## [003] ハッシュ関数の3つの性質
- **日付:** 2026-02-18(水)
- **カテゴリ:** セキュリティ / 暗号技術
- **プロジェクト:** 情報処理安全確保支援士
- **自分の理解:**
  - 衝突発見困難性：メッセージからハッシュ関数を作った時にそのハッシュ関数と同じハッシュ関数を作るためにメッセージを探すことが困難
  - 第二現像計算困難性：メッセージからハッシュ関数を作った時に、別でハッシュ関数を探し続けて同じメッセージを見つけることが難しい
  - 原像計算困難性：ハッシュ関数から元のメッセージを算出することが難しい
- **正誤:** 一部修正
- **補足:**
  - 用語の修正：
    - 「ハッシュ関数を作る」→「ハッシュ値を計算する」
    - 「第二現像」→「第二原像」
  - 正しい理解：
    1. **原像計算困難性（一方向性）**
       - ハッシュ値 → メッセージを求めるのが困難
       - 実務: パスワード保存
    2. **第二原像計算困難性（弱衝突耐性）**
       - メッセージM1とそのハッシュ値H1が与えられた時、H1 = hash(M2)となる別のM2を見つけるのが困難
       - 実務: デジタル署名
    3. **衝突発見困難性（強衝突耐性）**
       - hash(M1) = hash(M2)となる任意のM1, M2のペアを見つけるのが困難
       - 実務: デジタル証明書
  - 難易度: 原像 > 第二原像 > 衝突（衝突は誕生日攻撃で意外と簡単）
  - アルゴリズムの現状：
    - SHA-1: 非推奨（衝突発見困難性が破られた）
    - SHA-2（SHA-256, SHA-384, SHA-512）: 現在の主流
    - SHA-3: 新世代だが性能が良いわけではない（SHA-2の代替案）
    - MD5: 非推奨だが一部で使用
- **実務での使用頻度:** 高（パスワードハッシュ、JWT署名、証明書検証で毎日使用）
- **関連:** [001]（証明書検証でSHA-256が使われる）, [002]（HMAC-SHA256がJWT署名に使われる）

---

## [004] CRYPTREC暗号リスト
- **日付:** 2026-02-18(水)
- **カテゴリ:** セキュリティ / 暗号技術
- **プロジェクト:** 情報処理安全確保支援士
- **自分の理解:**
  - 電子政府推奨暗号リスト：安全性の実績が十分で、政府が推奨する暗号技術
  - 推奨候補暗号リスト：安全性は検証できているが、実績がないもの
  - 運用監視暗号リスト：昔は使われていたが今は安全性が欠けているので、既存システムで使われている時のみ監視の対象
- **正誤:** 一部修正（運用監視リストの説明を修正）
- **補足:**
  - 運用監視リストの正確な位置づけ：「まだ推奨できない」ではなく「かつて推奨していたが、安全性に懸念が生じた技術」
  - 新規採用はNG、既存システムは移行を促す
  - 試験で使えるまとめ：
    - 推奨：安全・実績あり → 新規採用OK
    - 推奨候補：安全・実績少ない → 新規採用OK
    - 運用監視：かつて推奨→今は懸念 → 新規採用NG
  - 運用監視リストの代表例：SHA-1
- **実務での使用頻度:** 中（暗号方式を選定する際の基準として参照）
- **関連:** [003]（SHA-1が運用監視リストに該当）

---

## [005] ベーシック認証
- **日付:** 2026-02-19(木)
- **カテゴリ:** セキュリティ / 認証
- **プロジェクト:** 情報処理安全確保支援士
- **自分の理解:** ベーシック認証は平文でユーザーIDとパスワードを送信して認証する。盗聴されると簡単に漏洩するため、TLS通信で暗号化する必要がある。TLSではDH法が使われている。
- **正誤:** 一部修正
- **補足:**
  - 「平文で送信」→ 正確には **Base64エンコード** して送信（ただし暗号化ではなく変換なので、デコードすれば即座に元の文字列に戻せる＝実質平文）
  - TLS + DH法の接続は正しい理解
  - 保護の流れ：ベーシック認証（Base64エンコード）→ TLS（ECDH → AES）で暗号化 → 安全に送信
- **実務での使用頻度:** 中（現在はより安全なトークン認証に移行しつつあるが、Basic認証は今も一部で使われる）
- **関連:** [002]（TLSのECDHと接続）

---

## [006] S/Key（チャレンジレスポンス認証）
- **日付:** 2026-02-24(火)
- **カテゴリ:** セキュリティ / 認証
- **プロジェクト:** 情報処理安全確保支援士
- **自分の理解:**
  1. クライアントがマスターパスワードとシード値をN回ハッシュ化したものと、シード値をサーバーに登録する
  2. クライアントがサーバーに接続要求を出す
  3. サーバーは登録時に貰ったシード値とシーケンス番号をチャレンジとしてクライアントに送付する
  4. クライアントはマスターパスワードと貰ったシード値をシーケンス回数分ハッシュ化してワンタイムパスワードを生成する
  5. ハッシュ値をレスポンスとしてサーバーに返す
  6. サーバーは受け取ったOTPを1回ハッシュ化して登録済みのハッシュ値と比較し認証する
  7. シーケンスが0になったら新しいシードで最初から再登録する
- **正誤:** 正解（議論を経て理解）
- **補足:**
  - ワンタイムである理由：シーケンス番号が毎回1ずつ減るため、同じOTPは2度使えない
  - S/Keyで防げる攻撃：リプレイ攻撃（過去のOTPの再利用）
  - S/Keyで防げない攻撃：中間者攻撃（リアルタイムの横取り）→ TLSの役割
  - 再登録時は必ず新しいシードで登録する（シードを使い回すと過去の盗聴OTPが再利用される危険がある）
- **実務での使用頻度:** 低（現在はTOTP等に置き換わっているが、試験頻出）
- **関連:** [005]（ベーシック認証との対比）, [003]（ハッシュの一方向性が核心）

---

## [007] 生体認証（FRR/FARのトレードオフ）
- **日付:** 2026-02-25(水)
- **カテゴリ:** セキュリティ / 認証
- **プロジェクト:** 情報処理安全確保支援士
- **自分の理解:**
  - 生体認証には指紋認証、静脈認証、虹彩認証、顔認証がある
  - 本人拒否率（FRR）と他人受け入れ率（FAR）はトレードオフ
  - 厳しくするとFRR（本人拒否率）が上がり、緩くするとFAR（他人受け入れ率）が上がる
  - 弱点：漏洩しても変更できない、スプーフィング攻撃、環境・体調による誤動作、コスト・選定の難しさ
- **正誤:** 一部修正
- **補足:**
  - FRR=FARとなる点を **EER（等エラー率）** といい、システムの精度比較に使用
  - 最大の弱点：生体情報はパスワードと違い漏洩後に変更不可能
- **実務での使用頻度:** 高（スマートフォン・入退室管理・本人確認で広く使用）
- **関連:** [005]（認証方式として）, [006]（OTPと組み合わせた多要素認証）

---

## [008] リスクベース認証
- **日付:** 2026-02-25(水)
- **カテゴリ:** セキュリティ / 認証
- **プロジェクト:** 情報処理安全確保支援士
- **自分の理解:** 異なる環境・IP・端末でログインしたときに追加認証を行う方式。「いつもと違う状況」を検知してリスクに応じて認証を強化する。
- **正誤:** 正解
- **補足:**
  - リスク判定要素：異なるIP・端末・時間帯・場所・行動パターン
  - 弱点：いつもと同じ環境（同じIP・端末）からの攻撃は検知できない（盗んだ端末・VPN偽装等）
- **実務での使用頻度:** 高（ネットバンキング・企業認証で広く採用）
- **関連:** [007]（生体認証と組み合わせた多要素認証として使用）

---